To compile the code move in the folder `build'':

$ cmake ..
$ make

To execute the code:

$ ./project

The program takes as optional argument the path of the folder containing the three datasets. By default the path is set to "../../../pose_estimation_dataset"